name: 每週觸發 Netlify 建置

on:
  schedule:
    - cron: "0 1 * * 1"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: 通知 Netlify 重新建置
        run: curl -X POST -d '{}' "${{ secrets.NETLIFY_BUILD_HOOK }}"
